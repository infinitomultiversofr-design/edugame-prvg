# Push payload contract

Payload recomendado: `type`, `title`, `body_neutral`, `route`, `event_id`, `expires_at`.

Não incluir notas, ocorrências, risco, gabaritos, nomes de terceiros ou conteúdo que não possa aparecer em tela bloqueada.
